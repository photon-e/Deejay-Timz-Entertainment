import type { NextAuthOptions } from 'next-auth';
import Credentials from 'next-auth/providers/credentials';
export const authOptions:NextAuthOptions={session:{strategy:'jwt'},providers:[Credentials({name:'Admin Login',credentials:{email:{},password:{}},async authorize(credentials){if(credentials?.email===process.env.ADMIN_EMAIL&&credentials?.password===process.env.ADMIN_PASSWORD){return{id:'admin',name:'Administrator',email:credentials.email,role:'Admin'} as any}return null;}})],callbacks:{jwt({token,user}){if(user)(token as any).role=(user as any).role;return token},session({session,token}){(session.user as any).role=(token as any).role;return session}}};
