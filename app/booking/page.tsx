import { Hero,Section } from '@/components/ui';import { categories,packages } from '@/lib/data';import { metadata } from '@/lib/seo';export const generateMetadata=()=>metadata('Book An Event');export default function Page(){return <><Hero title="Book Your Event" subtitle="Tell us about your date, location, guest count, package, and special requests."/><Section title="Booking Progress"><div className="mb-8 grid gap-3 md:grid-cols-4">{['Details','Package','Review','Confirmation'].map((s,i)=><div className="rounded-full bg-white/10 p-3" key={s}><span className="text-primary">0{i+1}</span> {s}</div>)}</div><form action="/api/bookings" method="post" className="grid gap-4 md:grid-cols-2"><input name="name" placeholder="Name"/><input name="email" placeholder="Email"/><input name="phone" placeholder="Phone"/><select name="eventType">{categories.map(c=><option key={c}>{c}</option>)}</select><input name="date" type="date"/><input name="location" placeholder="Location"/><input name="guests" type="number" placeholder="Number Of Guests"/><select name="package">{packages.map(p=><option key={p.name}>{p.name}</option>)}</select><textarea name="notes" placeholder="Special Requests" className="md:col-span-2"/><button className="rounded-full bg-primary px-6 py-3 font-bold text-black">Submit Booking</button></form></Section></>}
