'use client';import { useState } from 'react';import { categories } from '@/lib/data';import { GalleryGrid,Hero,Section } from '@/components/ui';export default function Page(){const [cat,setCat]=useState('All');return <><Hero title="Event Gallery" subtitle="Images, videos, albums, event categories, lightbox-ready previews, and performance moments."/><Section title="Professional Media Gallery"><div className="mb-8 flex flex-wrap gap-3">{['All',...categories].map(c=><button onClick={()=>setCat(c)} className={`rounded-full px-4 py-2 ${cat===c?'bg-primary text-black':'bg-white/10'}`} key={c}>{c}</button>)}</div><GalleryGrid/><p className="mt-6 text-white/60">Click-to-expand lightbox and Cloudinary video assets are ready for dashboard-managed uploads.</p></Section></>}
